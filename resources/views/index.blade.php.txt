<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resume List</title>
</head>
<body>
    <h1>Resumes</h1>
    <a href="{{ route('resume.create') }}">Create New Resume</a>
    <ul>
        @foreach ($resumes as $resume)
            <li>{{ $resume->name }} - {{ $resume->email }}</li>
        @endforeach
    </ul>
</body>
</html>
